name: Code Validation
run-name: ${{github.actor}} is running the github actions

## Basically pipeline will run on two types of events

on: 
  push:
    paths:
      - 'force-app/**'
    branches:
      - feature/*
      - bugfix/*
      - hotfix/*
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest 

    steps:
      - run: echo "This is my first-job"
        name: "This is my first job message"
      - name: checkout code
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      - name: Install SFDX CLI
        run: npm install sfdx-cli --global
      
      - name: Authorize the salesforce org using the JWT authentication through connected apps
        run: sf org login jwt --username sumatt1617-r3ch@force.com.dev --jwt-key-file assets/server.key --client-id 3MVG9cbS6twd6G6dwBRhkHIFHWkh4hShEz7R58uppUddxhTQEKl1m1_RNB2n.eOeYENtZftB_ND3gJJMrm1ao --alias ci-org --set-default --instance-url ${{secrets.SF_INSTANCE_URL}}
    
      ##- name: validate the code to salesforce
      ##  run: sf project deploy validate --source-dir force-app --target-org ci-org  

      - name: Deploy the code to salesforce
        run: sf project deploy start --source-dir force-app --target-org ci-org 
  clean-up:
    runs-on: ubuntu-latest
    needs: [build-and-deploy]

    steps:
      - run: echo "This is my second job"
        name: echo "This is my second job message"